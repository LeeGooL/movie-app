import React from "react";

import styles from "./favorite-list.module.scss";

const FavoriteList = ({ history }) => {
  console.log(history);

  return <h1>Favorite list</h1>;
};

export default FavoriteList;
